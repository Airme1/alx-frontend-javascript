export default function createReportObject(employeesList) {
  const allEmployees = ...employeesList;
  const numberOfDepartments = employeesList.length;
  return { allEmployees, numberOfDepartments };
}
